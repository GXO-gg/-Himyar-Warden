# Copy this file to ".env" and fill in your bot token.
# Never commit the real ".env" file to git.

DISCORD_TOKEN=your-bot-token-here

# Optional: restrict slash-command sync to one server while developing,
# so commands update instantly instead of waiting up to an hour for the
# global command cache to refresh. Leave blank for the public/global bot.
DEV_GUILD_ID=
